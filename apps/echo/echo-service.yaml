---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: ${instance:=echo}
  name: ${instance:=echo}
spec:
  ports:
    - port: "${port:=8080}"
      protocol: TCP
      targetPort: 8080
  selector:
    app: ${instance:=echo}

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis-operator
spec:
  chart:
    spec:
      chart: redis-operator
      version: 0.26.0
      sourceRef:
        kind: HelmRepository
        name: redis-operator
  driftDetection:
    mode: enabled
  interval: 15m
  timeout: 5m

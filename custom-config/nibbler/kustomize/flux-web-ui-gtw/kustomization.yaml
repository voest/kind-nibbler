---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
namespace: flux-system
configMapGenerator:
  - name: custom-vars-flux-web-ui-gtw
    envs:
      - values.env
    options:
      disableNameSuffixHash: true
      labels:
        reconcile.fluxcd.io/watch: Enabled

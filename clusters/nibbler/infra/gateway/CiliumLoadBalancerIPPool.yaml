---
apiVersion: "cilium.io/v2"
kind: CiliumLoadBalancerIPPool
metadata:
  name: "main-gateway-pool"
  namespace: infra-gateway
spec:
  blocks:
    # Configure unused IPs from the kind subnet on the host
    # Check with: `nerdctl network inspect kind`
    - cidr: "10.4.1.240/28"
